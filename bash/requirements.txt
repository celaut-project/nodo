requests<2.31.0
tabulate==0.9.0
# >=6.0.0 is where the manylinux aarch64 wheel appears. Below it pip builds psutil
# from source on ARM, and the portable CPython records CC=clang in sysconfig, so an
# install with only gcc dies with "No such file or directory: 'clang'".
psutil==6.1.1
netifaces2==0.0.16
ecdsa==0.19.0
python-dotenv==1.0.0
mnemonic==0.21
bip32==4.0
PyYAML==6.0.1
git+https://github.com/bee-rpc-protocol/bee-rpc-over-grpc-py
git+https://github.com/reputation-systems/ergpy
